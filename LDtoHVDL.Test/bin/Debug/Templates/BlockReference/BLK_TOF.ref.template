component BLK_TOF is
    generic (CLK_frequency : in integer);
    port (VAR_READ        : in  plc_time;
          VAR_WRITE       : out plc_time;
          EN              : in  std_logic;
          ENO             : out std_logic;
          PT              : in  plc_time;
          ET              : out plc_time;
          WRITE_CONDITION : out std_logic);
end component;